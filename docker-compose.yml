version: "3.8"

services:
  backend:
    build:
      context: .
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      LOG_LEVEL: INFO
      CORS_ORIGINS: http://localhost:3000,http://127.0.0.1:3000
    ports:
      - "8000:8000"
    volumes:
      - .:/app
